# Runtime deps for agent-dispatch scripts ONLY.
#
# These are NOT part of the epicoracle-feedback Python package
# (substrate consumers don't need LLM SDKs at runtime — those are
# only used in CI when the agent-dispatch workflow fires).
#
# setup.sh installs these into the workflow runner's venv after
# satellite deps land.
#
# Pinned versions: bump intentionally; substrate release + CHANGELOG.

anthropic>=0.40,<1.0
# openai>=1.50,<2.0           # uncomment when fix_pr.py + trinity_dispatch.py wire Codex
# google-generativeai>=0.8    # uncomment when trinity_dispatch.py wires Gemini
